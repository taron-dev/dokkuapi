package controller

import (
	"net/http"
)

// PostServices creates services for application
func PostServices(response http.ResponseWriter, request *http.Request) {
	// authorize user
	// parse body - type and name
	// dokku creates services
	// dokku links services
	//store to db
	//respond
}
